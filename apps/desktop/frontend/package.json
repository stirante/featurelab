{
  "name": "featurelab-desktop-frontend",
  "private": true,
  "version": "0.1.0",
  "description": "Wails webview entry point for the Feature Lab desktop app -- a thin toolbar (open pack, pick feature/rule, environment/seed/origin/size controls) around the shared featurelab-frontend voxel viewer.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "featurelab-frontend": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
