<!-- GENERATED by docs/site/tools/extract-catalog.mjs from apps/vscode/src/graph/typeCatalog.ts and
     apps/vscode/src/graph/docs/catalog.ts. Do not edit: edit those, re-run `npm run generate` here.
     Type: minecraft:nether_cave_carver_feature -->

#### `fill_with` {#fill_with}

<p class="fl-facts">optional · block · absent: no fill (the carve leaves the cell empty)</p>

**The block written into each carved cell.**

Leaving it out does NOT stop the carve: the cave is still shaped, sand is still capped and grass is still relocated onto the dirt below -- only the write itself is skipped, so the carved cells keep whatever was already there.

#### `width_modifier` {#width_modifier}

<p class="fl-facts">optional · molangOrNumber · absent: 0.0</p>

**Widens or narrows the tunnel.**

A plain number is used as-is. A Molang string is evaluated for real -- once per room, and again at every step of a tunnel -- against a generator that is not tied to the world seed, so a non-constant expression previews a faithful EXAMPLE of the shape rather than the shape this seed will produce in game.

A number is read directly. A Molang string is compiled and evaluated per carve -- and because the engine evaluates it against a generator that is not derived from the world seed, a non-constant expression previews a faithful EXAMPLE of the shape rather than the shape the game will produce for this seed.

#### `skip_carve_chance` {#skip_carve_chance}

<p class="fl-facts">optional · integer · absent: 0</p>

**A one-in-N gate on the whole carve, not a percentage.**

The carver draws a number below this value and proceeds only when that draw is zero, so the carve runs about one chunk-attempt in N and is skipped the rest of the time. Bigger means RARER. An explicit 0 is reported as failing the schema's own minimum of 1.

#### `height_limit` {#height_limit}

<p class="fl-facts">optional · integer · absent: 0</p>

**Caps how high a carve may reach.**

The carved volume's ceiling is the lower of this and two below the world top. Mind the default: an absent height_limit is 0, not "unlimited", which pins every carve near Y=0.

#### `y_scale` {#y_scale}

<p class="fl-facts">optional · range · absent: {0, 0}</p>

**Scales a room's vertical size, drawn once per room from this range.**

A zero-width range contributes nothing and costs no draw.

#### `horizontal_radius_multiplier` {#horizontal_radius_multiplier}

<p class="fl-facts">optional · range · absent: {0, 0}</p>

**Multiplies the horizontal radius of each carve, drawn once per carve from this range.**

A zero-width range contributes nothing and costs no draw.

#### `vertical_radius_multiplier` {#vertical_radius_multiplier}

<p class="fl-facts">optional · range · absent: {0, 0}</p>

**Multiplies the vertical radius of each carve, drawn once per carve from this range.**

A zero-width range contributes nothing and costs no draw.

#### `floor_level` {#floor_level}

<p class="fl-facts">optional · range · absent: {0, 0}</p>

**Flattens the bottom off each carved ellipsoid.**

Measured as a fraction of the ellipsoid's own vertical radius, from -1 at the very bottom to +1 at the top: every cell at or below this height is left uncarved. The default of 0 is not neutral -- it removes the entire lower half of every carve. Write -1 for a full ellipsoid.
